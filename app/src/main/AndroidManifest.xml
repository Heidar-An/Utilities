<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.notesapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotesApplication.NoActionBar">
        <activity android:name=".MainActivity">

            <!-- android:label="@string/app_name" -->
            <!-- android:theme="@style/Theme.NotesApplication.NoActionBar" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateNoteActivity">

            <!-- android:theme="@style/Theme.NotesApplication.NoActionBar" -->
        </activity>
        <activity android:name=".alarmFiles.AlarmActivity">

        </activity>

        <activity android:name=".alarmFiles.CreateAlarmActivity">
            
        </activity>

        <receiver android:name=".alarmFiles.AlarmReciever" android:exported="false"/>
    </application>

</manifest>